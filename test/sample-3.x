/*This is an example */

integer function (param5:real, param2:integer)
var b:integer; /*c:real; d:integer;*/

begin
LeftAssn = (call(0.44 , 15))*(plusParam1 + plusParam2)/(minus1 - minus2);
if a==b || c<d || e<>f then x=y;
for (i=0;i<size;i=i+1;)
	read (book);
while !(tierd==tierd) || ran<yoni do
		return (coffie); 
if first==exp then if second==exp2 then return (first); else write (letter);

end
program
var a:integer;
begin
	/*if I<Y then if a == b then A=B; else read(AB); else write(IY);*/

end



